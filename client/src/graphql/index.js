import client from "./client";
import { GET_USERS, GET_POSTS, GET_COMMENTS, GET_TODOS, CHECK_TODO, GET_USER } from "./queries";

export { 
    client,
    GET_USERS,
    GET_POSTS,
    GET_COMMENTS,
    GET_TODOS,
    CHECK_TODO,
    GET_USER
}
